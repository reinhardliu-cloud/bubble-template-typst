---
name: rs-benchmark
description: Alias command for /gstack-benchmark. Use when user asks for /rs-benchmark or wants the same workflow as /gstack-benchmark.
---

# /rs-benchmark

This skill is an alias wrapper.

1. Load and follow the original skill at ~/.codex/skills/gstack-benchmark/SKILL.md.
2. Execute the same workflow and quality bar as /gstack-benchmark.
